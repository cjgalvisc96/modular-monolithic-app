# Argo CD Application — todo-app on the prod floci-EKS (k3s) cluster. The EKS
# cluster runs its own Argo CD (installed by the platform's eks:bootstrap, which
# the promote workflow calls) and syncs this Application in-cluster. See
# todo-app-dev.yaml for the deploy contract.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: todo-app-prod
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: prod
  source:
    repoURL: http://172.18.255.209:3000/gitops/modular-monolithic-app.git
    targetRevision: HEAD
    path: infra/k8s/helm
    helm:
      releaseName: todo-app
      valueFiles:
        - values.yaml
        - values-prod.yaml
        - values-floci.yaml
      parameters:
        - name: namespaceConfig.create
          value: "false"
  destination:
    server: https://kubernetes.default.svc
    namespace: todo-app
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions: [CreateNamespace=true, ApplyOutOfSyncOnly=true]
    retry:
      limit: 5
      backoff: { duration: 10s, factor: 2, maxDuration: 3m }
