name: Promote to prod

# Manual promotion to PROD (workflow_dispatch). Takes a tag already proven on
# dev (defaults to whatever values-dev.yaml currently points at), makes sure
# that image exists locally — pulling it back from the floci ECR if the dev
# build host has since pruned it — kind-loads it into the prod cluster, then
# bumps infra/k8s/helm/values-prod.yaml and commits. Argo CD on prod syncs.
#
# This is the only path that changes prod. No build happens here — the artifact
# was built by the cd job in ci-cd.yml and lives in the floci ECR.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Image tag to promote (default: current dev tag)"
        required: false
        default: ""

concurrency:
  group: promote
  cancel-in-progress: false

permissions:
  contents: write

env:
  APP_IMAGE: local/todo-app
  ECR_REPO: gitops/todo-app
  AWS_ACCESS_KEY_ID: test
  AWS_SECRET_ACCESS_KEY: test
  AWS_REGION: us-east-1
  AWS_ENDPOINT_URL: http://localhost:4566

jobs:
  promote:
    runs-on: lab
    steps:
      - uses: actions/checkout@v4

      - uses: ./.gitea/actions/setup-lab

      - name: Resolve tag
        id: vars
        run: |
          set -euo pipefail
          tag="${{ github.event.inputs.tag }}"
          if [ -z "$tag" ]; then
            tag="$(yq '.image.tag' infra/k8s/helm/values-dev.yaml)"
          fi
          if [ -z "$tag" ] || [ "$tag" = "null" ]; then
            echo "no tag to promote (pass one or run CD on dev first)" >&2
            exit 1
          fi
          echo "Promoting tag: $tag"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Ensure the image is available locally
        run: |
          set -euo pipefail
          tag="${{ steps.vars.outputs.tag }}"
          if ! docker image inspect "$APP_IMAGE:$tag" >/dev/null 2>&1; then
            REPO_URI=$(aws --endpoint-url "$AWS_ENDPOINT_URL" ecr describe-repositories \
              --repository-names "$ECR_REPO" --query 'repositories[0].repositoryUri' --output text)
            aws --endpoint-url "$AWS_ENDPOINT_URL" ecr get-login-password \
              | docker login --username AWS --password-stdin "${REPO_URI%%/*}"
            docker pull "$REPO_URI:$tag"
            docker tag "$REPO_URI:$tag" "$APP_IMAGE:$tag"
          fi

      - name: Load image into the prod cluster
        run: kind load docker-image "$APP_IMAGE:${{ steps.vars.outputs.tag }}" --name prod

      - name: Bump values-prod.yaml and commit
        run: |
          set -euo pipefail
          tag="${{ steps.vars.outputs.tag }}"
          yq -i ".image.tag = \"$tag\"" infra/k8s/helm/values-prod.yaml
          if git diff --quiet -- infra/k8s/helm/values-prod.yaml; then
            echo "values-prod.yaml already at $tag — nothing to commit"
            exit 0
          fi
          git config user.name "gitea-actions"
          git config user.email "actions@gitops.local"
          git add infra/k8s/helm/values-prod.yaml
          git commit -m "ci: promote $tag to prod [skip ci]"
          scheme="${GITHUB_SERVER_URL%%://*}"
          host="${GITHUB_SERVER_URL#*://}"
          git push "${scheme}://x-access-token:${{ github.token }}@${host}/${GITHUB_REPOSITORY}.git" HEAD:main
